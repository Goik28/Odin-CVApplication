import React, { Component } from "react";

export class Education extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
        <form action="">
        <fieldset>
            <legend>Education</legend>
        <button>Add Education Info</button>
        <ol></ol>
        </fieldset>
        </form>
    );
  }
}
